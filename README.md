# terraform_webserver
aws infrastructure with nginx webservers and autoscaling using terraform

## AWS Credentials

If you run Terraform from PowerShell on Windows, use `cred.ps1` instead of `cred.sh`:

```powershell
. .\cred.ps1
terraform init
```
